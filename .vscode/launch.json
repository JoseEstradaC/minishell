{
    "configurations": [
        {
            "name": "Launch",
            "type": "lldb",
            "request": "launch",
            "preLaunchTask": "compile",
            "program": "${workspaceFolder}/minishell",
            "args": [],
        }
    ]
}